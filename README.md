python ml chat
text -> tfidf -> linear svm classificator
Support Vectors (SVs) are the closest ones points to the border
the border for classes
hiper surface in multidimensional space between those classes 
tfidf transforms text into vector, weight, with each iteration inside a document of same exact vector, tfidf stays the same and svm adapts the border
